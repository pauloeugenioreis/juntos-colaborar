apiVersion: apps/v1
kind: Deployment
metadata:
  name: juntos-colaborar-function
  labels:
    app: juntos-colaborar-function
spec:
  replicas: 1
  selector:
    matchLabels:
      app: juntos-colaborar-function
  template:
    metadata:
      labels:
        app: juntos-colaborar-function
    spec:
      containers:
      - name: juntos-colaborar-function
        image: juntos-colaborar-function:latest
        ports:
        - containerPort: 80
        env:
        - name: FUNCTIONS_WORKER_RUNTIME
          value: "dotnet-isolated"
        - name: RabbitMQConnectionString
          value: "amqp://guest:guest@rabbitmq.local:5672"
        - name: AzureWebJobsDisableHomepage
          value: "true"
        resources:
          requests:
            memory: "256Mi"
            cpu: "250m"
          limits:
            memory: "512Mi"
            cpu: "500m"
---
apiVersion: v1
kind: Service
metadata:
  name: juntos-colaborar-service
spec:
  selector:
    app: juntos-colaborar-function
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80
  type: ClusterIP